# NanoGridBot 前端改造设计方案

> 日期: 2026-02-21
> 状态: 设计完成，待实施
> 定位: Agent 开发调试控制台（非聊天平台）

---

## 一、设计目标

将从 HappyClaw 引入的前端改造为 NanoGridBot 专用的 **Agent 开发调试控制台**：

- **轻量快速** — 桌面优先，减少页面跳转
- **信息集中** — IDE 风格多面板布局，调试信息同屏可见
- **交互增强** — 对话形式与 CC 交互，但具备更强的数据展示能力
- **品牌统一** — 清除所有 HappyClaw 引用

---

## 二、整体布局：IDE 风格四面板

```
┌──────────────────────────────────────────────────────────────┐
│ NanoGridBot    [workspace-name ▼]   [metrics]   [⚙️] [👤]   │  Top Bar
├─────────┬──────────────────────────┬─────────────────────────┤
│         │                          │                         │
│  W      │   Conversation Stream    │    Inspector Panel      │
│  o      │                          │                         │
│  r      │  ┌ thinking... ────────┐ │  ┌─ Tool Call ────────┐ │
│  k      │  │ (collapsible)       │ │  │ Bash: pytest -xvs  │ │
│  s      │  └─────────────────────┘ │  │ exit: 0            │ │
│  p      │  [user message]          │  │ stdout: 12 passed  │ │
│  a      │  [tool: Edit app.py]     │  └────────────────────┘ │
│  c      │  [code diff inline]      │  ┌─ Session Info ─────┐ │
│  e      │  [agent response]        │  │ Tokens: 12.3k      │ │
│  s      │                          │  │ Duration: 45s       │ │
│         │                          │  │ Container: abc123   │ │
│  ────   │                          │  └────────────────────┘ │
│  + New  │  [input area      ⏎]    │                         │
├─────────┴──────────────────────────┴─────────────────────────┤
│  Container Output  │  IPC Stream  │  Metrics                  │
│  $ claude --prompt "..."                                      │
│  [real-time stdout/stderr stream]                             │
└──────────────────────────────────────────────────────────────┘
```

**核心原则**：对话是中心，但被上下文包围。开发者无需离开就能看到所有调试信息。

---

## 三、面板职责

### 3.1 左侧 Workspace 列表（~200px，可折叠）

- 当前用户的 Workspace 列表（`GET /api/user/groups`）
- 每个 Workspace 显示：名称、状态指示灯（idle/running/error）、未读消息数
- 底部「+ New」按钮创建 Workspace
- 点击切换，对话区自动加载该 Workspace 的消息流
- 替代现有的 GroupsPage 和 ChatSidebar

### 3.2 中间对话流（主区域）

CC 交互的完整信息流，按时间顺序排列。消息类型区分渲染：

| 消息类型 | 渲染方式 |
|---------|---------|
| **User Message** | 简洁气泡 |
| **Thinking** | 折叠块，点击展开推理链，浅灰底色 |
| **Tool Call** | 紧凑卡片：工具名 + 关键参数摘要，点击在 Inspector 展开 |
| **Agent Response** | Markdown 渲染，代码块语法高亮 |
| **Code Diff** | 内嵌 diff 查看器（Edit 的 old_string → new_string） |
| **Error** | 红色边框卡片 |

底部输入框：多行，Shift+Enter 换行，Enter 发送。

### 3.3 右侧 Inspector（~300px，可折叠）

点击对话流中的消息 → Inspector 显示该消息的详情：

- **Tool Call 详情**：完整输入参数 + 完整输出结果，可复制
- **Thinking 详情**：完整推理文本
- **Session 元数据**：session_id、token 消耗、执行时长、容器 ID
- **文件变更汇总**：当前会话中所有 Read/Edit/Write 操作的文件列表
- 未选中消息时显示 Workspace 概览信息

### 3.4 底部面板（可折叠，默认 ~150px，三个 Tab）

| Tab | 内容 |
|-----|------|
| **Container Output** | 容器 stdout/stderr 实时流，滚动跟随 |
| **IPC Stream** | IPC 消息的原始 JSON 流，开发调试用 |
| **Metrics** | 当前 Workspace 的容器执行统计（替代 MonitorPage） |

---

## 四、页面裁剪方案

### 4.1 现有页面处理

| 现有页面 | 处理 | 说明 |
|---------|------|------|
| LoginPage | ✅ 保留，换品牌 | 去 HappyClaw，换 NanoGridBot logo |
| RegisterPage | ✅ 保留，换品牌 | 同上 |
| SetupPage | ✅ 保留，换品牌 | 首次初始化向导 |
| SetupProvidersPage | ✅ 保留 | Claude API 配置 |
| SetupChannelsPage | ⏸️ 暂保留 | IM 通道设计待定 |
| ChatPage | 🔄 **改造为 Debug Console** | 核心改造，四面板布局 |
| GroupsPage | ❌ 删除 | 合并到左侧 Workspace 列表 |
| MonitorPage | ❌ 删除 | 合并到底部 Metrics Tab + Top Bar |
| TasksPage | ⏸️ 暂保留 | 后续可考虑合并到 Console |
| MemoryPage | ⏸️ 暂保留 | 后续可考虑合并 |
| SkillsPage | ⏸️ 暂保留 | 设计待定 |
| SettingsPage | ✅ 保留，精简 | Profile + Appearance + Channels |
| UsersPage | ✅ 保留 | Admin 专用 |

### 4.2 路由简化

```
/login          → 登录
/register       → 注册
/setup          → 首次初始化
/               → Debug Console（默认主页）
/tasks          → 任务管理（暂保留）
/memory         → 记忆管理（暂保留）
/skills         → Skills 管理（暂保留）
/settings       → 设置
/admin          → 用户管理 + 审计日志
```

### 4.3 导航精简

左侧 NavRail 只保留：
- 🏠 Console（主页）
- ⚙️ Settings
- 👥 Admin（admin 角色才显示）
- Tasks/Memory/Skills 作为次要入口

**移除底部 Tab Bar** — 桌面优先的开发工具不需要移动端底部导航。

---

## 五、品牌清理清单

需要替换的文件和操作：

| 文件 | 操作 |
|------|------|
| `index.html` | title → "NanoGridBot" |
| `src/main.tsx` | `__HAPPYCLAW_HASH_ROUTER__` → `__NGB_HASH_ROUTER__` |
| `src/vite-env.d.ts` | 同步类型定义 |
| `src/utils/url.ts` | 同步变量名 |
| `src/api/adapter.ts` | 注释中 HappyClaw → NanoGridBot |
| `src/pages/LoginPage.tsx` | 标题、footer、logo alt |
| `src/pages/RegisterPage.tsx` | 同上 |
| `src/pages/ChatPage.tsx` | 欢迎语、logo alt、fallback name |
| `src/pages/SetupPage.tsx` | 标题文案 |
| `src/components/layout/NavRail.tsx` | logo alt |
| `src/components/chat/ChatSidebar.tsx` | fallback name |
| `src/components/settings/AboutSection.tsx` | 项目信息、GitHub 链接 |
| `src/components/settings/AppearanceSection.tsx` | placeholder |
| `public/` | 替换 favicon.svg、icon-192.png、apple-touch-icon |
| `vite.config.ts` | PWA 名称 |

---

## 六、待定事项（后续研究）

以下事项需要进一步设计，本次不纳入实施范围：

1. **User ↔ Workspace 关系模型** — 当前 Group 概念是否直接映射为 Workspace
2. **IM 通道管理** — 通道与 Workspace 的关联方式
3. **Skills 管理 UI** — 是否合并到 Debug Console
4. **Tasks 位置** — 独立页面还是 Console 内面板
5. **Memory 位置** — 独立页面还是 Console 内面板

---

## 七、实施分期建议

### Phase A：品牌清理（确定可做）
- 替换所有 HappyClaw 引用
- 更新 logo、favicon、PWA 配置
- 不改功能，不改布局

### Phase B：Debug Console 核心（确定可做）
- ChatPage 改造为四面板布局
- 对话流增强渲染（Thinking/Tool Call/Diff/Error）
- Inspector 面板
- 底部面板（Container Output）

### Phase C：页面整合（确定可做）
- 删除 GroupsPage，合并到 Workspace 列表
- 删除 MonitorPage，合并到 Console
- 导航精简，移除底部 Tab Bar

### Phase D：待定功能（需进一步研究）
- User/Workspace 关系确定后调整数据模型
- IM 通道管理重新设计
- Skills/Tasks/Memory 的最终位置
